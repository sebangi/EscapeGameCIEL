<form method="post" action="{{ path('app_variable_de_controle_delete', {'id': variable_de_controle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ variable_de_controle.id) }}">
    <button class="btn">Delete</button>
</form>
