<div class="m-4">
{{ form_start(form) }}   
    
    <div class="row justify-content-start custom-line m-2"> 
        <div class="col-1 mon_form_label">Numéro</div>
        <div class="col-4">{{ form_widget(form.numero) }}</div>
    </div>    
    <div class="row justify-content-start custom-line m-2"> 
        <div class="col-1 mon_form_label">Nom</div> 
        <div class="col-4">{{ form_widget(form.nom) }}</div>   
    </div>
    <div class="row justify-content-start custom-line m-2"> 
        <div class="col-1 mon_form_label">Description</div> 
        <div class="col-8">{{ form_widget(form.description) }}</div>   
    </div>
        
    <div class="row justify-content-start custom-line m-2"> 
        <div class="col-5">
            <button class="btn btn-success">{{ button_label|default('Ajouter') }}</button>
            <a href="{{ path('app_mecanisme_index') }}" class="btn btn-danger">
                Annuler
            </a>
        </div>   
    </div>      
            
{{ form_end(form) }}
</div>