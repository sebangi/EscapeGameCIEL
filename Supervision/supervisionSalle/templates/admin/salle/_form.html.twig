<div class="m-4">
{{ form_start(form) }}   
    
    <div class="row justify-content-start custom-line m-2"> 
        <div class="col-4 mon_form_label">Numéro</div>
        <div class="col-6">{{ form_widget(form.numero) }}</div>
    </div>    
    <div class="row justify-content-start custom-line m-2"> 
        <div class="col-4 mon_form_label">Nom</div> 
        <div class="col-6">{{ form_widget(form.nom) }}</div>   
    </div>
    <div class="row justify-content-start custom-line m-2"> 
        <div class="col-4 mon_form_label">Description</div> 
        <div class="col-6">{{ form_widget(form.description) }}</div>   
    </div>
        
    <div class="row justify-content-start custom-line m-2"> 
        <div class="col-4"></div> 
        <div class="col-6">
            <button class="btn btn-success">{{ button_label|default('Ajouter') }}</button>
        
            {% if creation == "true" %}
                <a href="{{ path('app_admin_salle_index') }}" class="btn  btn-secondary">
            {% else%}
                <a href="{{ path('app_admin_salle_show', {'id': salle.id}) }}" class="btn  btn-secondary">
            {% endif %}
                Annuler
            </a>
        </div>   
    </div>      
            
{{ form_end(form) }}
</div>